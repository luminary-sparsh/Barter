<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>-->
    </style>


    <style name="ButtonStyle">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Theme.AppCompat.Light is not a dialog theme -->
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="AlertDialogTheme" parent="AlertDialog.AppCompat.Light">
        <item name="android:windowIsFloating">true</item>
        <item name="buttonBarPositiveButtonStyle">@style/Base.Widget.AppCompat.Button.Borderless.PositiveButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/Base.Widget.AppCompat.Button.Borderless.NegativeButtonStyle</item>
    </style>
    
    <style name="Base.Widget.AppCompat.Button.Borderless.PositiveButtonStyle">
        <item name="android:textColor">@color/red_delete</item>
    </style>
    <style name="Base.Widget.AppCompat.Button.Borderless.NegativeButtonStyle">
        <item name="android:textColor">@color/black_text</item>
    </style>
</resources>
